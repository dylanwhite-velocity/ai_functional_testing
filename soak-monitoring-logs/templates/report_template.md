# Soak Monitoring Report Template
# 
# This template is used by Copilot to generate standardized reports.
# Edit this template to customize the output format.
#
# Available variables (populated from log analysis):
#   {{date}}                    - Report date
#   {{time_range}}              - Time window analyzed
#   {{total_items}}             - Total Velocity items checked across all environments
#   {{items_with_errors}}       - Number of items with errors
#   {{total_velocity_errors}}   - Total Velocity API errors found
#   {{total_pod_errors}}        - Total pod log errors found
#   {{model_name}}              - LLM model used for report generation
#   {{environments}}            - List of environments monitored (see fields below)
#   {{items}}                   - List of items with errors (see item fields below)
#
# Environment fields:
#   {{env.sut_name}}            - Instance name
#   {{env.organization_id}}     - Org ID
#   {{env.username}}            - Username
#   {{env.rating}}              - Stoplight rating: GREEN, YELLOW, RED (pre-computed)
#   {{env.rating_reasons}}      - Reasons for the assigned rating
#
# Item fields:
#   {{item.item_id}}            - Velocity item ID
#   {{item.item_name}}          - Velocity item name  
#   {{item.item_type}}          - Type: feed, realtime_analytic, bigdata_analytic
#   {{item.velocity_error_count}} - Number of Velocity API errors
#   {{item.pod_error_count}}    - Number of pod log errors
#   {{item.pod_name}}           - Associated pod name
#   {{item.pod_status}}         - Pod status (Running, Error, etc.)
#   {{item.restart_count}}      - Pod restart count
#   {{item.errors}}             - List of error messages

---

# Velocity Soak Monitoring Report

**Date:** {{date}}  
**Time Range:** {{time_range}}

### Environments Monitored

| Status | Instance | Org ID | Username |
|--------|----------|--------|----------|
{{#each environments}}
| {{rating_indicator}} {{rating}} | {{sut_name}} | {{organization_id}} | {{username}} |
{{/each}}

_Ratings: :green_circle: GREEN = healthy, :yellow_circle: YELLOW = degraded, :red_circle: RED = critical_

---

## Executive Summary

{{executive_summary}}

---

## Health Status

| Metric | Value |
|--------|-------|
| Total Items Monitored | {{total_items}} |
| Items with Errors | {{items_with_errors}} |
| Error Rate | {{error_rate}}% |
| Velocity API Errors | {{total_velocity_errors}} |
| Pod Log Errors | {{total_pod_errors}} |

**Overall Status:** {{overall_status}}

---

## Items Requiring Attention

_Items are grouped by environment (instance / org ID / username)._

{{#each environments_with_errors}}
### Environment: {{sut_name}} / {{organization_id}} / {{username}}

{{#each items}}
#### {{item_name}} ({{item_type}})

| Property | Value |
|----------|-------|
| Item ID | `{{item_id}}` |
| Pod | `{{pod_name}}` |
| Pod Status | {{pod_status}} |
| Restarts | {{restart_count}} |
| Velocity Errors | {{velocity_error_count}} |
| Pod Errors | {{pod_error_count}} |

**Error Summary:**
{{error_summary}}

**Sample Errors (with timestamps):**
```
{{#each errors limit=5}}
{{this}}
{{/each}}
```

**Recommended Action:**
{{recommendation}}

---
{{/each}}
{{/each}}

## Recommendations

{{#each recommendations}}
{{@index}}. {{this}}
{{/each}}

---

*Report generated by soak-monitoring-logs | Model: {{model_name}}*
